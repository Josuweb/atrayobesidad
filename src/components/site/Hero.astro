---
export interface Props {
  title: string;
  text: string;
  primaryLabel: string;
  primaryHref: string;
  secondaryLabel?: string;
  secondaryHref?: string;
}

const { title, text, primaryLabel, primaryHref, secondaryLabel, secondaryHref } = Astro.props;
---
<section class="bg-gradient-to-br from-blue-50 via-white to-blue-100">
  <div class="mx-auto flex max-w-6xl flex-col gap-6 px-4 py-16 sm:px-6 lg:flex-row lg:items-center lg:gap-12 lg:py-20 lg:px-8">
    <div class="flex-1 space-y-6">
      <p class="text-sm font-semibold uppercase tracking-wide text-blue-700">ATRA y Obesidad Salamanca</p>
      <h1 class="text-3xl font-bold text-slate-900 sm:text-4xl lg:text-5xl">{title}</h1>
      <p class="text-lg leading-relaxed text-slate-700">{text}</p>
      <div class="flex flex-wrap gap-3">
        <a
          class="inline-flex items-center justify-center rounded-lg bg-blue-700 px-5 py-3 text-sm font-semibold text-white shadow-md transition hover:bg-blue-800"
          href={primaryHref}
        >
          {primaryLabel}
        </a>
        {secondaryLabel && secondaryHref && (
          <a
            class="inline-flex items-center justify-center rounded-lg border border-blue-700 px-5 py-3 text-sm font-semibold text-blue-700 transition hover:bg-blue-50"
            href={secondaryHref}
          >
            {secondaryLabel}
          </a>
        )}
      </div>
    </div>
    <div class="flex-1">
      <div class="overflow-hidden rounded-2xl bg-white p-6 shadow-lg ring-1 ring-blue-100">
        <p class="text-base font-semibold text-blue-700">Acompañamiento cercano</p>
        <p class="mt-3 text-sm leading-relaxed text-slate-700">
          Nuestro equipo voluntario escucha, orienta y conecta a las familias con recursos especializados. Desde 2019
          trabajamos para que ninguna persona afectada por un Trastorno de la Conducta Alimentaria o la obesidad se sienta sola.
        </p>
        <div class="mt-6 grid grid-cols-2 gap-4 text-sm font-semibold text-slate-800">
          <div class="rounded-lg bg-blue-50 px-4 py-3">Prevención y sensibilización</div>
          <div class="rounded-lg bg-blue-50 px-4 py-3">Orientación y derivación</div>
          <div class="rounded-lg bg-blue-50 px-4 py-3">Acompañamiento familiar</div>
          <div class="rounded-lg bg-blue-50 px-4 py-3">Participación comunitaria</div>
        </div>
      </div>
    </div>
  </div>
</section>
